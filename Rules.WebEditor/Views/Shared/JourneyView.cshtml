@using Rules.Domain
@model Rules.WebEditor.Models.JourneyViewModel

<div>
    <div>Journeys</div>
    
    @for (int i = 0; i < Model.Blades.Count; i++)
    {
        <div>@Html.Partial("BladeView", Model.Blades[i])</div>
    }

</div>