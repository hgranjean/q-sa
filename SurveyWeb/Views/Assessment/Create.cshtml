@model MvcApplication1.Models.AssessmentViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm("Save", "Assessment")) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <fieldset>
        <legend>Create assessment Form</legend>
        <ol>
            <li>
                @Html.EditorFor(m => m.Enumerator.Current)
            </li>
        </ol>
        <input type="submit" value="Submit questionnnaire" />
    </fieldset>
}
